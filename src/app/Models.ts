export type AddExpenditure = {
    amount: string;
    description: string;
    division: string;
    category: string;
    date: string;
}

export type AddIncome = {
    amount: string;
    description: string;
    date: string
}